# Development-only values.
# Copy this to development.cfg in the ignored server-data directory.
# MariaDB 12.3.2 runs as TarrantMariaDB on loopback; XAMPP is unsupported.

setr tarrant_environment "development"

# Placeholders only. Replace them in the ignored copy, never in this example.
set mysql_connection_string "host=127.0.0.1;port=3306;user=tarrant_rp;password=CHANGE_ME;database=tarrant_rp_dev;charset=utf8mb4"
sv_licenseKey "CHANGE_ME"
