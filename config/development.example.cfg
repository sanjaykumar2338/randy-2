# Development-only values.
# Copy this to config/development.cfg and keep the real file out of Git.
# Qbox currently requires MariaDB 10.9+; XAMPP MariaDB 10.4 is not sufficient.

setr tarrant_environment "development"

# Placeholder only. Replace CHANGE_ME in ignored local config.
set mysql_connection_string "mysql://tarrant_rp:CHANGE_ME@127.0.0.1:3306/tarrant_rp_dev?charset=utf8mb4"
